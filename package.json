{
	"name": "clanker",
	"version": "1.0.0",
	"description": "- gpt-4o for its sycophantness\r - variety of tasks",
	"license": "ISC",
	"author": "",
	"type": "module",
	"main": "tasks.js",
	"scripts": {
		"start": "powershell -Command \"Remove-Item Env:OPENAI_API_KEY; node --env-file=.env tasks.js\"",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"dependencies": {
		"openai": "^5.16.0"
	}
}
